pipeline {
    agent {
        docker { image 'ubuntu' }
    }
    stages {
        stage('Test') {
            steps {
             	sh 'cd /tmp; vi test_jen2'
		sh ' echo "abc" > test_jen2'
		sh 'cat test_jen2'
		
            }
        }
    }
}

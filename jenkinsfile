pipeline {
    agent {
        docker { image 'ubuntu' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'apt-get update'
		sh 'cd /tmp; vi test_jen2'
            }
        }
    }
}
